<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModelBase="clr-namespace:MyHealth.Mobile.Core.ViewModels.Base;assembly=MyHealth.Mobile.Core"
             mc:Ignorable="d"
             x:Class="MyHealth.Mobile.Core.Views.ObservationsView"
             viewModelBase:ViewModelLocator.AutoWireViewModel="true">
    <ContentPage.Content>
        <StackLayout>
            <CollectionView ItemsSource="{Binding Observations, Mode=TwoWay}"
                            VerticalOptions="StartAndExpand"
                            HorizontalOptions="FillAndExpand"/>
            <Editor Text="{Binding Observation.Value, Mode=TwoWay}" 
                    VerticalOptions="FillAndExpand"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Add"
                    Command="{Binding AddObservationCommand}"
                    VerticalOptions="End"
                    HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
